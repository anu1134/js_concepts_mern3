// function sum(a, b) {
//   return a + b;
// }

// ES6 features
const sum = (a, b) => a + b;

const result = sum(2, 3);

console.log(result);

function isPositive(number) {
  return number >= 0;
}

const isPositive = (number) => number >= 0;
